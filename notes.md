# 开发规则

面向参与本项目的开发者与 AI 编码助手。以下流程为强制要求。

## 开发流程（必须按序执行）

1. **设计方案对齐** — 动手前先给出设计，等待开发者明确确认（"好的"／"可以"／"确认"）
2. **测试用例对齐** — 确认测试方法、测试场景与成功标准
3. **编码实现**
4. **运行测试** — 测试不通过不得通知开发者，先修复
5. **通知开发者验证**

若开发者未提供设计方案，必须自己先思考设计并交付对齐，**不得跳过确认直接编码**。

禁止行为：

- 提出方案后未获确认就开始编码，或假设开发者会同意
- 编码前未讨论测试用例，边开发边想怎么测试
- 测试未通过就通知开发者验证

确认为准的设计方案与实现细节，完成后必须写入 `plan.md`（新增接口的用途与参数、与现有接口的区别、关键技术点与设计决策），目的是防止后续修改破坏已有设计。

## 分支管理

- **永远不要直接提交到 `main`**
- 改动前先创建分支：`git checkout -b <branch-name>`
- 命名建议：`feat/xxx`、`fix/xxx`、`docs/xxx`、`refactor/xxx`

## 功能保护

- 改动前先判断是否会破坏已有功能；会影响已有功能时**必须先询问开发者**
- 不得擅自破坏或删除已有功能，除非开发者明确要求
- 修改代码时考虑向后兼容性

## 规划阅读

每次开始新任务前先读 `plan.md`，了解整体架构与既有决策，避免重复摸索和前后不一致。

## 代码规范

- 遵循项目现有代码风格，格式以 `.clang-format` 为准（`./scripts/check-format.sh` 可检查与自动修复）
- 修改前先阅读相关代码，理解现有实现
- 不要改动明确标注为禁止修改的代码（当前仓库中尚无此类标注）
